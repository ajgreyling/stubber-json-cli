{
  "name": "stubber-json",
  "version": "1.0.0",
  "description": "Explode / implode Stubber template JSON for file-based editing",
  "license": "Unlicense",
  "type": "module",
  "bin": {
    "stubber-json": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "node --experimental-vm-modules node_modules/vitest/vitest.mjs run"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3",
    "vitest": "^1.2.2"
  }
}
